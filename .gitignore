node_modules/
dist/
*.js.map
.DS_Store
.env
.env.*
!.env.example
ingest/backups/

afy.db
ingest/afy.db

# stray WXT scaffold (we build with esbuild, not wxt)
extension/.wxt/
model.json
ingest/.avatars/

# local-only
ducky-cli/
.claude/
.omx/
# auto-generated CLAUDE.md mirror (its find-replace garbles the rubric-scorer facts — do not publish)
AGENTS.md
